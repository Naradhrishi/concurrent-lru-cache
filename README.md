# concurrent-lru-cache
Thread-safe LRU cache in Java with striped ReentrantReadWriteLock, Node based TTL eviction, and lock-free metrics — built to demonstrate concurrency, race condition prevention, and high-throughput synchronization.
